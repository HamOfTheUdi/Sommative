<%= form_with(model: proprietaire) do |form| %>
  <% if proprietaire.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(proprietaire.errors.count, "error") %> prohibited this proprietaire from being saved:</h2>

      <ul>
        <% proprietaire.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :nom, style: "display: block" %>
    <%= form.text_field :nom %>
  </div>

  <div>
    <%= form.label :email, style: "display: block" %>
    <%= form.text_field :email %>
  </div>

  <div>
    <%= form.submit %>
  </div>
<% end %>
